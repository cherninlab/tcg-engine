import { Container, Title } from '@mantine/core';

function ProfileSettingsPage() {
	return (
		<Container>
			<Title>Profile Settings</Title>
			{/* TODO: Implement profile settings interface */}
		</Container>
	);
}

export default ProfileSettingsPage;
